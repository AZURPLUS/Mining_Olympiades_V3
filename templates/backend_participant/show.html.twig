{% extends 'base.html.twig' %}

{% block title %}Participant{% endblock %}

{% block body %}
    <h1>Participant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ participant.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ participant.nom }}</td>
            </tr>
            <tr>
                <th>Prenoms</th>
                <td>{{ participant.prenoms }}</td>
            </tr>
            <tr>
                <th>Matricule</th>
                <td>{{ participant.matricule }}</td>
            </tr>
            <tr>
                <th>Contact</th>
                <td>{{ participant.contact }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ participant.email }}</td>
            </tr>
            <tr>
                <th>Media</th>
                <td>{{ participant.media }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ participant.slug }}</td>
            </tr>
            <tr>
                <th>Licence</th>
                <td>{{ participant.licence }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backend_participant_index') }}">back to list</a>

    <a href="{{ path('app_backend_participant_edit', {'id': participant.id}) }}">edit</a>

    {{ include('backend_participant/_delete_form.html.twig') }}
{% endblock %}
